let config = {
  service: 'http://localhost:59652/api/',
  clientId: '***************************************',
  authority: '*************************************************************'
};

export default config;
